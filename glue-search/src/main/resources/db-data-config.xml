<dataConfig>
	<dataSource type="JdbcDataSource" driver="com.mysql.jdbc.Driver"
		url="jdbc:mysql://localhost/gluedb" user="glue" password="glue" />
	<document name="streams">
		<entity name="stream" query="select * from STREAM">
			<entity name="venue"
				query="select name as venue, address, latitude, longitude from VENUE where id='${stream.venue_id}'">
			</entity>
		</entity>
	</document>
</dataConfig>

